

  Pipes have two main targets:

    1) transformation
    2) validation

  1. Transform input data to the desired output.
  2. Evaluate input data and if valid, simply pass it through unchanged; 
    otherwise, throw an exception when the data is incorrect.